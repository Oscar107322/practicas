let a =3,b = 2,C=1,d=4;
//let z = a * b + C;
//console.log( z )
//z = C + a * b;
//console.log( z );
 let z = a * b + C / d;  
 console.log( z );
 z = C + a * b / d
 console.log( z );

 z =( C + a) * b/C;
 console.log( z );
